# App database
APP_DB_NAME=anchor
APP_DB_USER=anchor
APP_DB_PASSWORD=anchor
APP_DB_ROOT_PASSWORD=anchor_root

# Billing database
BILLING_DB_NAME=anchor_billing
BILLING_DB_USER=anchor_billing
BILLING_DB_PASSWORD=anchor_billing
BILLING_DB_ROOT_PASSWORD=anchor_billing_root

# Mail (Mailpit on localhost:1025 for local dev; override for SMTP relay / SES in production)
MAIL_HOST=localhost
MAIL_PORT=1025
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_SMTP_AUTH=false
MAIL_SMTP_STARTTLS=false
MAIL_FROM=noreply@anchor.local
